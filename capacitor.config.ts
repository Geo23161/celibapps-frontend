import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'celibapps.elif.com',
  appName: 'Celibapps',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
